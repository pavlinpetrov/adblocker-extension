{
  "name": "adblocker-extension",
  "version": "2.1.7",
  "description": "",
  "scripts": {
    "build": "webpack --mode=production",
    "buildMajor": "npm run build && npm --no-git-tag-version version major",
    "buildMinor": "npm run build && npm --no-git-tag-version version minor",
    "buildPatch": "npm run build && npm --no-git-tag-version version patch",
    "dev": "webpack --watch --mode=development",
    "archive": "webpack --mode=production && node scripts/archive"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/preset-env": "^7.9.5",
    "archiver": "^3.1.1",
    "babel-loader": "^8.1.0",
    "clean-webpack-plugin": "^3.0.0",
    "copy-webpack-plugin": "^5.1.1",
    "webpack": "^4.42.1",
    "webpack-auto-inject-version": "^1.2.2",
    "webpack-cli": "^3.3.11"
  }
}
